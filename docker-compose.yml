version: "3.9"

services:

  youtube-rag-chatbot:

    build: .

    container_name: youtube-rag-chatbot

    ports:
      - "8000:8000"

    env_file:
      - .env

    restart: unless-stopped

    volumes:
      - ./data:/app/data